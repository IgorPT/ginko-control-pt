

        {{#if (content "home")}}
            {{{block "home"}}}
        {{/if}}

        {{#if (content "users")}}
            {{{block "users"}}}
        {{/if}}

        {{#if (content "permissions")}}
            {{{block "permissions"}}}
        {{/if}}

        {{#if (content "menus")}}
            {{{block "menus"}}}
        {{/if}}

        {{#if (content "contenttypes")}}
            {{{block "contenttypes"}}}
        {{/if}}
